backend:
  name: github
  repo: gempeler/blog-markus-gempeler

media_folder: static/assets
public_folder: assets

collections:
  - name: blog
    label: Blog
    folder: blog
    create: true
    fields:
      - { name: path, label: Pfad }
      - { name: date, label: Datum, widget: date }
      - { name: title, label: Title }
      - { name: subtitle, label: Untertitel}
      - { name: body, label: Text, widget: markdown }